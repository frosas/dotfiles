#!/bin/bash

# A `git diff` of $BRANCH (`HEAD` by default) against $UPSTREAM_BRANCH ('origin/master' by default)

BRANCH=${1:-"HEAD"}
UPSTREAM_BRANCH=${2:-"origin/master"} # TODO How to obtain it automatically?

git diff $UPSTREAM_BRANCH...$BRANCH "${@:3}"
