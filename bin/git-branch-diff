#!/bin/bash

# A `git diff` of $BRANCH (`HEAD` by default) against $PARENT_BRANCH ('origin/master' by default)

BRANCH=${1:-"HEAD"}
PARENT_BRANCH=${2:-"origin/master"} # TODO How to obtain it automatically?

git diff $PARENT_BRANCH...$BRANCH "${@:3}"
