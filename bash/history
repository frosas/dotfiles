# vim: filetype=bash

# http://briancarper.net/blog/248/ 
# https://news.ycombinator.com/item?id=10162189

# Note: use `history -n` to update the history from another terminal

shopt -s histappend # Append instead of overwriting
HISTSIZE="50000"
HISTCONTROL=erasedups # Experimental

update_history () { history -a; } # "append history lines from this session to the history file"
PROMPT_COMMAND="update_history; $PROMPT_COMMAND"